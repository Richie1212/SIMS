import AppShell from "@/components/AppShell";

export default function ExpiryTrackerPage() {
  return (
    <AppShell>
      <div>
        <h1 className="text-xl font-bold sm:text-2xl">
          <span className="text-(--color-text-primary)">Inventory/</span>
          <span className="text-(--color-text-muted)"> Expiry tracker</span>
        </h1>
        <p className="text-sm text-(--color-text-muted)">Coming soon — build this from its Figma frame next.</p>
      </div>
    </AppShell>
  );
}
